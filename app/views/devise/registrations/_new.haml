%h2= t('.sign_up')
= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| 
	= devise_error_messages!
	.input-field
		= f.label :email
		%br/
		= f.email_field :email, autofocus: true
	.input-field	
		= f.label :password 
		%br/
		= f.password_field :password, autocomplete: "off" 
	.input-field
		= f.label :password_confirmation
		%br/
		= f.password_field :password_confirmation, autocomplete: "off"

	.input-field
		-Tipo.all.each do |tipo|
			= f.radio_button :tipo_id,tipo.id, checked:"checked"
			= f.label "tipo_id_#{tipo.id}", tipo.tipo_de_usuario.humanize
			&nbsp;&nbsp;&nbsp;&nbsp;
	.input-field
		= f.submit t('.sign_up')


-#=form_for(Publicacion.new) do |f|
	-# -#@perfil.errors.full_messages.each do |mensaje|
	-# 	=mensaje
	-# 	%br/
	-# .field
	-# 	=f.email_field :titulo
	-# .field
	-# 	= f.submit "Publicar"



-#= render "devise/shared/links"
