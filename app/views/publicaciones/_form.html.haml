= form_for @publicacion do |f|
	.row
		.input-fiedl.col.s12
			- if @publicacion.errors.any?
				%blockquote = "#{pluralize(@publicacion.errors.count, "error")} ocurrio al guardar publicar"
			- @publicacion.errors.full_messages.each do |msg|
				%li= msg
				%script Materialize.toast("#{msg}", 10000);
		.input-field.col.s12
			= f.label :titulo
			= f.text_field :titulo
		.input-field.col.s12
			= f.label :mensaje
			= f.text_area :mensaje,class:"materialize-textarea", data:{length:"400"}
		.col.s12.input-field
			%i.material-icons.prefix today
			= f.label :fecha_de_termino
			= f.date_field :fecha_de_termino,class:"datepicker"
		.col.s12
			.file-field.input-field
				.btn
					%span
						%i.material-icons attach_file
					%input{type:"file", multiple:""}
				.file-path-wrapper
					%input.file-path.validate{type:"text", placeholder:"Archivos a subir"}
			%h5 Seleccione la prioridad del comunicado
			.col.s12.input-field
				= f.check_box :prioridad
				= f.label :prioridad,"Urgente"
		-#===============Boton publicar================
	.fixed-action-btn
		%a.btn-floating.btn-large.waves-effect.waves-light.red.tooltipped.hoverable{href:"#mensaje-confirmar", data:{position:"left", delay:"50", tooltip:"Publicar"}}
			%i.material-icons done
	-#===============Mensaje del boton=============
	#mensaje-confirmar.modal
		.modal-content
			%h4 ¿Deseas continuar?
			%p Revise los datos, y asegurese de que son correctos.
			%p.right
				%a.modal-action.modal-close.waves-effect.waves-green.btn-flat{href:"#"} Volver
				= f.submit 'Publicar',class:"modal-action modal-close  waves-effect waves-green btn"
- if @formato_erroneo
   %div
      %p El formato ingresado es incorrecto. Solo puedes subir archivos .doc, .docx, .pdf, .xlsx, .pptx, .ppt, .xls